# Python side of the verification harness.
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
#
# The GLSL side needs glslang (glslangValidator + spirv-dis), which is not a
# Python package:
#   brew install glslang
#
# Pinned, not floated. Several gates here are exact byte comparisons between two
# GPU renders, and the golden hashes in tools/baseline.toml are only meaningful
# against a fixed stack, so a change in moderngl's defaults is indistinguishable
# from a shader regression. Bump deliberately, and re-record the goldens.
numpy==2.5.1
moderngl==5.12.0
Pillow==12.3.0
