[pytest]
# Run from the repo root: .venv/bin/python -m pytest
#
# tools/ and tools/tests/ are both on the path so tests can import the harness
# (core.*, models.*) and each other's helpers without a package dance.
testpaths = tools/tests
pythonpath = tools tools/tests
python_files = *_test.py
addopts = -q
